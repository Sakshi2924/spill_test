"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TimelineVideoInfo = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
const media_parser_1 = require("@remotion/media-parser");
const webcodecs_1 = require("@remotion/webcodecs");
const react_1 = require("react");
const remotion_1 = require("remotion");
const timeline_layout_1 = require("../../helpers/timeline-layout");
const HEIGHT = timeline_layout_1.TIMELINE_LAYER_HEIGHT - 2;
const containerStyle = {
    height: HEIGHT,
    width: '100%',
    backgroundColor: 'rgba(0, 0, 0, 0.3)',
    display: 'flex',
    borderTopLeftRadius: 2,
    borderBottomLeftRadius: 2,
    fontSize: 10,
    fontFamily: 'Arial, Helvetica',
};
const MAX_TIME_DEVIATION = media_parser_1.WEBCODECS_TIMESCALE * 0.05;
const calculateTimestampSlots = ({ visualizationWidth, fromSeconds, segmentDuration, aspectRatio, }) => {
    const framesFitInWidth = Math.ceil(visualizationWidth / (HEIGHT * aspectRatio));
    const timestampTargets = [];
    for (let i = 0; i < framesFitInWidth; i++) {
        timestampTargets.push(fromSeconds +
            ((segmentDuration * media_parser_1.WEBCODECS_TIMESCALE) / framesFitInWidth) *
                (i + 0.5));
    }
    return timestampTargets;
};
const createSlots = ({ filledSlots, frameDatabase, visualizationWidth, fromSeconds, toSeconds, }) => {
    if (filledSlots.size > 0) {
        return;
    }
    const anyFrame = frameDatabase.values().next().value;
    if (!anyFrame) {
        return;
    }
    const segmentDuration = toSeconds - fromSeconds;
    const aspectRatio = anyFrame.displayWidth / anyFrame.displayHeight;
    const timestampTargets = calculateTimestampSlots({
        visualizationWidth,
        fromSeconds,
        segmentDuration,
        aspectRatio,
    });
    for (const timestamp of timestampTargets) {
        if (!filledSlots.has(timestamp)) {
            filledSlots.set(timestamp, undefined);
        }
    }
};
const fillWithCachedFrames = ({ ctx, frameDatabase, visualizationWidth, filledSlots, }) => {
    const keys = Array.from(frameDatabase.keys());
    const targets = Array.from(filledSlots.keys());
    for (let i = 0; i < filledSlots.size; i++) {
        const timestamp = targets[i];
        let bestKey;
        let bestDistance = Infinity;
        for (const key of keys) {
            const distance = Math.abs(key - timestamp);
            if (distance < bestDistance) {
                bestDistance = distance;
                bestKey = key;
            }
        }
        if (!bestKey) {
            continue;
        }
        const frame = frameDatabase.get(bestKey);
        if (!frame) {
            continue;
        }
        const alreadyFilled = filledSlots.get(timestamp);
        // Don't fill if a closer frame was already drawn
        if (alreadyFilled &&
            Math.abs(alreadyFilled - timestamp) <=
                Math.abs(frame.timestamp - timestamp)) {
            continue;
        }
        ctx.drawImage(frame, (i / filledSlots.size) * visualizationWidth, 0);
        filledSlots.set(timestamp, frame.timestamp);
    }
};
const fillFrameWhereItFits = ({ frame, filledSlots, ctx, visualizationWidth, }) => {
    const slots = Array.from(filledSlots.keys());
    for (let i = 0; i < slots.length; i++) {
        const slot = slots[i];
        if (Math.abs(slot - frame.timestamp) > MAX_TIME_DEVIATION) {
            continue;
        }
        const filled = filledSlots.get(slot);
        // Don't fill if a better timestamp was already filled
        if (filled &&
            Math.abs(filled - slot) <= Math.abs(filled - frame.timestamp)) {
            continue;
        }
        ctx.drawImage(frame, (i / filledSlots.size) * visualizationWidth, 0);
        filledSlots.set(slot, frame.timestamp);
    }
};
const TimelineVideoInfo = ({ src, visualizationWidth, startFrom, durationInFrames }) => {
    const { fps } = (0, remotion_1.useVideoConfig)();
    const ref = (0, react_1.useRef)(null);
    const [error, setError] = (0, react_1.useState)(null);
    const frameDatabase = (0, react_1.useMemo)(() => new Map(), []);
    (0, react_1.useEffect)(() => {
        if (error) {
            return;
        }
        const { current } = ref;
        if (!current) {
            return;
        }
        const canvas = document.createElement('canvas');
        canvas.width = visualizationWidth;
        canvas.height = HEIGHT;
        const ctx = canvas.getContext('2d');
        if (!ctx) {
            return;
        }
        // desired-timestamp -> filled-timestamp
        const filledSlots = new Map();
        const fromSeconds = startFrom / fps;
        const toSeconds = (startFrom + durationInFrames) / fps;
        createSlots({
            filledSlots,
            frameDatabase,
            visualizationWidth,
            fromSeconds,
            toSeconds,
        });
        fillWithCachedFrames({
            ctx,
            frameDatabase,
            visualizationWidth,
            filledSlots,
        });
        current.appendChild(canvas);
        const controller = new AbortController();
        (0, webcodecs_1.extractFrames)({
            acknowledgeRemotionLicense: true,
            timestampsInSeconds: ({ track }) => {
                const aspectRatio = track.width / track.height;
                const framesFitInWidth = Math.ceil(visualizationWidth / (HEIGHT * aspectRatio));
                const timestampTargets = [];
                const segmentDuration = toSeconds - fromSeconds;
                for (let i = 0; i < framesFitInWidth; i++) {
                    timestampTargets.push(fromSeconds + (segmentDuration / framesFitInWidth) * (i + 0.5));
                }
                return timestampTargets;
            },
            src,
            onFrame: (frame) => {
                const scale = HEIGHT / frame.displayHeight;
                const transformed = webcodecs_1.WebCodecsInternals.rotateAndResizeVideoFrame({
                    frame,
                    resizeOperation: {
                        mode: 'scale',
                        scale,
                    },
                    rotation: 0,
                    needsToBeMultipleOfTwo: false,
                });
                if (transformed !== frame) {
                    frame.close();
                }
                const existingFrame = frameDatabase.get(transformed.timestamp);
                if (existingFrame) {
                    existingFrame.close();
                }
                frameDatabase.set(transformed.timestamp, transformed);
                createSlots({
                    filledSlots,
                    frameDatabase,
                    fromSeconds,
                    toSeconds,
                    visualizationWidth,
                });
                fillFrameWhereItFits({
                    ctx,
                    filledSlots,
                    visualizationWidth,
                    frame: transformed,
                });
            },
            signal: controller.signal,
        })
            .then(() => {
            fillWithCachedFrames({
                ctx,
                frameDatabase,
                visualizationWidth,
                filledSlots,
            });
        })
            .catch((e) => {
            if ((0, media_parser_1.hasBeenAborted)(e)) {
                return;
            }
            setError(e);
        });
        return () => {
            controller.abort();
            current.removeChild(canvas);
        };
    }, [
        durationInFrames,
        error,
        fps,
        frameDatabase,
        src,
        startFrom,
        visualizationWidth,
    ]);
    (0, react_1.useEffect)(() => {
        return () => {
            const entries = Array.from(frameDatabase.entries());
            for (const [, frame] of entries) {
                frame.close();
            }
            frameDatabase.clear();
        };
    }, [frameDatabase]);
    return (0, jsx_runtime_1.jsx)("div", { ref: ref, style: containerStyle });
};
exports.TimelineVideoInfo = TimelineVideoInfo;
